import {
  FiniteResource,
  RectArea,
  Transformation,
  diContainer,
} from "@curtain-call/util";

diContainer.register("Transformation", Transformation);

export { FiniteResource, RectArea, Transformation };
