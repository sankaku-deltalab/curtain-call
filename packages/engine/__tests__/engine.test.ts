import { Engine } from "../src";

describe("@curtain-call/engine.Engine", () => {
  it("needs tests", () => {
    // TODO: write test
    const engine = new Engine();
    expect(engine).not.toBeUndefined();
  });
});
